# مراجعة الحسابات وتجربة المستخدم (المحاسب / السكرتير)

التاريخ: 2026-09-12 — الإصدار المراجَع: 1.56.1 (`main` عند `8b3f3e1` + تعديلات السجل غير الملتزَمة في `server.js`/`logger.js`).

المنهج: قراءة كاملة لمحرك الرسوم `public/fees.js`، طبقة الحفظ `db.js`، الصلاحيات في
`server.js`، وشاشات `students.js` / `fees-ui.js` / `staff.js` / `reports.js` /
`index.html`، مع تتبّع سير عمل السكرتير اليومي: **تسجيل طالب → قبض رسوم → طباعة
وصل → صرف راتب/سلفة → تسجيل مصروف → إقفال اليوم/الشهر**. كل ملاحظة مرقّمة
تحمل الملف والسطر، وما كُتب «تحقّقت» جُرّب فعليًا بسكربت على قاعدة مؤقتة.

الترميز: 🔴 خطأ محاسبي أو خطر على البيانات · 🟠 نقص يُربك المحاسب شهريًا · 🟢 تحسين.

---

## 0. الخلاصة (أهم خمس نقاط)

| # | الملاحظة | الأثر |
| --- | --- | --- |
| 1 | 🔴 لا يوجد إجراء لبداية سنة دراسية جديدة: تغيير «السنة الدراسية» يُعيد توزيع دفعات السنة الماضية على رسوم السنة الجديدة (تحقّقت). البديل الوحيد «تفريغ البيانات» يمسح كل التاريخ. | أرصدة خاطئة أو فقدان السجل |
| 2 | 🔴 حقل «الشهر» في الدفعة مجرد **تسمية**؛ التوزيع الفعلي يتجاهله (`allocate`). لكن نافذة «تعديل الدفعة» ومرشّح سجل التحصيل ولوحة «ملخص الشهر» تعامله كحقيقة. | أرقام الشهر على اللوحة تخالف التقرير |
| 3 | 🔴 الخادم لا يفرض سقف الراتب عند **الإضافة** ولا سقف السلفة إن غاب `salaryDue`، ولا يتحقق من شهر السلفة ولا من صيغة التاريخ (تحقّقت). | راتب 50 000 لموظف راتبه 10 000 يُقبل |
| 4 | 🟠 كشف رواتب الشهر يتجاهل تاريخ التوظيف ونهاية الخدمة، ويعيد حساب استحقاق الأشهر الماضية بالراتب الحالي. | موظف عُيّن في فبراير يظهر «لم يُصرف» لأكتوبر |
| 5 | 🟠 لا أثر لمن سجّل أو عدّل أو حذف؛ الحذف يمحو الوصل نهائيًا ويترك فجوة في التسلسل. | لا يمكن مراجعة عمل السكرتير |

---

## 1. الحسابات

### 🔴 1.1 بداية السنة الدراسية — `public/fees.js:134-137`, `db.js:577-588`

- `enrolmentIndex` يقصّ تاريخ تسجيل أقدم من أكتوبر إلى الشهر الأول. طالب سُجّل في
  2025-10-01 ودفع 24 200 في 2025/2026: بعد تغيير الإعداد إلى «2026 / 2027»
  تظهر دفعاته القديمة وقد سدّدت تسجيل ويونيو وأكتوبر من السنة الجديدة
  و«المتبقي: 0» (تحقّقت بـ `ledgerFor`).
- «تفريغ بيانات السنة الدراسية» (`clearOperationalData`) يحذف الطلاب والدفعات
  والموظفين والرواتب: يُجبر المدرسة على إعادة إدخال كل الطلاب والموظفين كل سنة
  ويُفقد تاريخ الوصولات.
- **المطلوب**: إجراء «إقفال السنة / فتح سنة جديدة» يحفظ نسخة، يُرحّل الطلاب
  النشطين بتاريخ تسجيل جديد (مع الترقية إلى القسم التالي اختياريًا)، يُبقي
  الموظفين، ويُؤرشف دفعات السنة المنتهية بحيث لا تدخل في توزيع السنة الجديدة
  (مثلًا حقل `schoolYear` على الدفعة، أو تصفية بتاريخ الدفعة ≥ بداية السنة).
  وإلى أن يُنفَّذ: تحذير صريح عند تغيير «السنة الدراسية» في الإعدادات.

### 🔴 1.2 «شهر الدفعة» تسمية لا حقيقة — `public/fees.js:187-206` — ✅ نُفِّذ الخيار الثاني في 1.59.0

`allocate` يرتّب الدفعات بالمعرّف ويوزّعها على أقدم رسم غير مسدَّد، ويتجاهل
`payment.month` تمامًا (تحقّقت: دفعة موسومة «مارس» سدّدت التسجيل ويونيو). هذا
مقصود ومكتوب في التعليق، لكن أربعة مواضع تتصرف كأن الحقل حقيقي:

| الموضع | المشكلة |
| --- | --- |
| نافذة «تعديل الدفعة» (`fees-ui.js:349-366`) | التعليق يقول إنها «تصحح الرسم الذي تسدده الدفعة»؛ تغيير الشهر لا يغيّر شيئًا في كشف الحساب. السكرتير يظن أنه صحّح ولم يصحّح. |
| مرشّح «نوع الرسوم / الشهر» في سجل التحصيل (`fees-ui.js:263`) | يصفّي على التسمية؛ دفعة واحدة غطّت ثلاثة أشهر تظهر تحت شهر واحد فقط. |
| لوحة «ملخص الشهر الجاري» (`reports.js:139-144`) | `dFees` = دفعات وسمها الشهر الجاري، بينما شاشة التقارير تحسب بتاريخ الدفعة. الرقمان يختلفان للشهر نفسه. |
| الوصل (`fees-ui.js:312-325`) | «نوع الرسوم: رسوم شهر X» و«إجمالي الرسوم» و«المتبقي لهذه الرسوم» تُقرأ من التسمية، ثم سطر «توزيع الدفعة الفعلي» يقول شيئًا آخر. ولي الأمر يرى وصلًا يناقض نفسه. |

**المطلوب**: إمّا جعل الحقل حقيقيًا (توزيع على الشهر المختار عندما تُحدَّد) أو
إزالته من واجهة التعديل والمرشّحات واعتماد التوزيع الفعلي (`allocations`) في
الوصل واللوحة والتصفية. الخيار الثاني أقل تغييرًا وأصدق.

### 🔴 1.3 تحققات ناقصة في الخادم — `db.js` — ✅ نُفِّذ في 1.58.0 (باستثناء كود `initialPaid` الميت، بقي كما هو)

الواجهة تمنع هذه الحالات، لكن الخادم هو خط الدفاع (طلب من نافذة قديمة، خطأ في
الواجهة، جهاز آخر على الشبكة). تحقّقت من كل سطر على قاعدة مؤقتة:

| الدالة | الفجوة |
| --- | --- |
| `addTeacherPayment` (`db.js:855`) | لا سقف: قُبل 50 000 لراتب 10 000. `updateTeacherPayment` وحدها تفرض السقف. `salaryDue` يأتي من المتصفح ويُخزَّن كما هو. |
| `addTeacherAdvance` (`db.js:882`) | إن غاب `salaryDue` (أو كان 0) فلا سقف: قُبلت سلفة 99 999. |
| `addTeacherAdvance` / `updateTeacherAdvance` | الشهر لا يُقارن بقائمة `MONTHS` («شهر وهمي» قُبل)؛ يجب `assertPaymentMonth`-مثيل للرواتب والسلف. |
| `addStudentPayment` / `updateStudentPayment` / الرواتب / السلف / المصروفات | التاريخ لا يُتحقق من صيغته `YYYY-MM-DD` (قُبل «31/10/2025»). كل المرشّحات والتقارير تقارن التاريخ نصًّا، فسجل بصيغة خاطئة يختفي من تقارير الشهر. |
| `updateExpense` (`db.js:908`) | يقبل مبلغ 0 بينما `addExpense` يرفضه. |
| `addStudent` (`db.js:664-711`) | كود `initialPaid` / `initialPaymentMonth` لا تستعمله الواجهة إطلاقًا (ميت) ويعتمد على `currentPaymentMonth()` بالتاريخ الحقيقي لا تاريخ الاختبار. يُحذف أو يُربط بالنموذج (انظر 2.1). |

### 🟠 1.4 كشف الرواتب وتاريخ الخدمة — `public/staff.js:66-68, 171-200, 426-431`

- `activeTeachers()` يصفّي بالحالة فقط. موظف `startDate = 2026-02-01` يظهر في
  كشف أكتوبر–يناير «لم يُصرف» ويُضاف راتبه إلى «إجمالي الاستحقاق» و«المتبقي».
- موظف متوقف يختفي من كشوف الأشهر التي عمل فيها فعلًا (دفعاته تبقى في السجل لكن
  كشف الشهر المطبوع يفقده).
- `salaryDue(t, m)` للراتب الثابت يقرأ `t.fixedSalary` الحالي لكل شهر: بعد زيادة
  الراتب في مارس تظهر الأشهر السابقة «جزئي» بمتبقٍّ وهمي. الدفعة تخزّن `salaryDue`
  وقت الصرف؛ يجب أن يُقرأ منها للأشهر المصروفة، ويُقرأ الراتب الحالي للأشهر غير
  المصروفة فقط.
- **المطلوب**: في `monthlyPayroll` استبعاد الشهر السابق لتاريخ التوظيف واللاحق
  لنهاية الخدمة، واعتماد `salaryDue` المخزَّن عند وجود دفعة.

### 🟠 1.5 لا أثر للمستخدم ولا إلغاء للوصولات

- لا سجل يحتوي `createdBy` / `updatedBy` رغم وجود حسابات متعددة (`db.js` لا
  يعرف المستخدم؛ `server.js` يعرفه في `session.user` ولا يمرّره).
- الحذف (`deleteStudentPayment`, `deleteTeacherPayment`, `deleteTeacherAdvance`,
  `deleteExpense`) يمحو السجل. الوصل المطبوع في يد ولي الأمر يصبح بلا أصل، ويظهر
  ثقب في التسلسل F-000123. محاسبيًا الصحيح: **إلغاء** (`cancelled`, السبب، من،
  متى) يبقى في السجل ويُستثنى من التوزيع والمجاميع.
- حذف طالب أو موظف يجرّ معه كل وصولاته (`deleteStudent`, `deleteTeacher`). يُفضَّل
  منع الحذف إن كانت له دفعات (كما يُمنع حذف قسم فيه طلاب) والاكتفاء بحالة
  «منقطع/متوقف».
- السكرتير يملك الحذف بكلمة مروره هو (`allowed()` في `server.js:120`). يُقترح
  جعل حذف/إلغاء الوصولات للمدير، أو على الأقل تسجيله في `logs/app.log` مع اسم
  المستخدم (السجل الجديد في `logger.js` مكان مناسب).

### 🟠 1.6 قواعد تستحق تأكيدًا من المدرسة (ليست أخطاء بالضرورة)

| القاعدة | الموضع | السؤال |
| --- | --- | --- |
| رسم يونيو يُفرض حتى على من غادر في ديسمبر | `fees.js:146-171` (تحقّقت) | مقصود كـ«ضمان» عند التسجيل؟ إن لم يكن، يجب استثناء يونيو إذا `leaveDate` قبل يونيو. |
| الرواتب تُسجَّل لأشهر أكتوبر–يونيو فقط | `core.js:282` (`setupMonths`) | لا يمكن صرف راتب يوليو/أغسطس/سبتمبر ولا سلفة صيفية. في الصيف يقترح النموذج «أكتوبر». |
| الراتب يُستحق في آخر يوم من الشهر، لكن التاريخ يُدخله المستخدم | `db.js:851` | يمكن تجاوز القاعدة بكتابة 31 في حقل التاريخ. مقبول؟ |
| الخصم لا يمس رسم التسجيل | `fees.js:125` | مطابق للنص في الشاشة؛ يبقى موثقًا. |
| «عدد الطلاب» على اللوحة و«ملخص المستحقات» يشملان المنقطعين والمتخرجين | `reports.js:129`, `184` | يُفضَّل عدّ النشطين وذكر الباقين منفصلين. |

---

## 2. تجربة السكرتير / المحاسب

### 🔴 2.1 تسجيل طالب جديد لا يقود إلى قبض التسجيل — `students.js:70-118`

بعد «حفظ الطالب» يُفرَّغ النموذج ويظهر toast فقط. السكرتير يجب أن يبحث عن الطالب
في الجدول ثم «المالية». في يوم التسجيلات هذا ضِعف الوقت لكل طالب.
**المطلوب**: بعد الحفظ فتح استمارة الرسوم مباشرة (`openStudentFees(id)`) أو زر
«حفظ وقبض الرسوم». (كود `initialPaid` في `db.js` كان لهذا ولم يُربط.)

### 🔴 2.2 القبض لا ينتهي بوصل — `students.js:417-437`

بعد «تسجيل الدفعة» لا يُعرض الوصل؛ يجب فتح «كشف الفواتير» ثم «طباعة». ولي الأمر
واقف أمام المكتب. **المطلوب**: زر «تسجيل وطباعة الوصل» (الافتراضي) بجانب
«تسجيل فقط»، وطباعة تلقائية بعد النجاح كما يفعل `printStudentReceipt` أصلًا.

### 🟠 2.3 استمارة الرسوم بلا ملاحظة ولا طريقة دفع

- الملاحظة ثابتة «دفعة موزعة تلقائيًا» (`students.js:430`)؛ لا يمكن كتابة «دفع
  العم» أو «بنكيلي» إلا بالتعديل بعد الحفظ (وبكلمة مرور).
- لا حقل «طريقة الدفع» (نقدًا / Bankily / Masrvi / تحويل) في الرسوم ولا في
  الرواتب ولا المصروفات، فلا يمكن مطابقة الصندوق النقدي مع الحساب البنكي في آخر
  اليوم.
- **المطلوب**: حقل ملاحظة اختياري وقائمة طرق دفع (تُضبط في الإعدادات كما
  `staffRoles`)، وعمود في سجل التحصيل والتقرير.

### 🟠 2.4 لا «يومية صندوق» ولا إقفال يوم

المحاسب يحتاج كل مساء: ما دخل اليوم (بالوصولات)، ما خرج (رواتب، سلف،
مصروفات)، والرصيد. اليوم:

- سجل التحصيل (`#collections`) يعرض المقبوضات فقط، بلا طباعة ولا تصدير
  (الشاشات الأخرى تملكهما).
- التقارير شهرية فقط (`reportPeriods`)، ولا ترحيل لرصيد الشهر السابق، ولا
  تصدير Excel.
- **المطلوب**: في «التقارير» فترة «اليوم» و«فترة مخصصة (من–إلى)» مع جدول
  الحركات (وصل، طالب/موظف، نوع، مبلغ، طريقة الدفع، المستخدم) وزر طباعة/تصدير،
  ورصيد افتتاحي = صافي ما قبل الفترة.

### 🟠 2.5 نوع المصروف نص حر — `index.html:324`, `reports.js:258`

«كهرباء» و«الكهرباء» و«كهربا» ثلاثة أنواع في «المصروفات حسب النوع». **المطلوب**:
قائمة أنواع في الإعدادات (كما «طبيعة العمل») مع إمكانية الإضافة، وحقل
`<datalist>` على الأقل في المرحلة الأولى.

### 🟠 2.6 الوصل الورقي — `fees-ui.js:290-347`

- تناقض التسمية/التوزيع (1.2). يُقترح: سطر واحد «سُدِّد به: التسجيل 200، يونيو
  11 800» بدل «نوع الرسوم» + «إجمالي الرسوم» + «المتبقي لهذه الرسوم».
- لا اسم للمستخدم الذي قبض (يرتبط بـ 1.5)؛ «توقيع المحاسب» فراغ.
- لا رقم هاتف المدرسة رغم وجوده في الإعدادات (`schoolPhone`).

### 🟠 2.7 طلب كلمة المرور لكل تعديل

`requirePassword()` يُستدعى عند: تعديل طالب، تعديل دفعة، تعديل مصروف، حفظ خصم،
وكل حذف. سكرتير يصحح خمسة أخطاء إدخال متتالية يكتب كلمة مروره خمس مرات. مع
سجل مستخدم (1.5) يمكن الاكتفاء بالتأكيد للحذف/الإلغاء وتغيير المبلغ، وترك
تصحيح الملاحظة/التاريخ بلا كلمة مرور.

### 🟢 2.8 تفاصيل أصغر

- `#feeMonth` و`#payrollMonth` و`#salaryMonth` تبدأ بـ`currentMonth()` بينما
  «التقارير» تبدأ بالشهر الجاري أيضًا؛ في الصيف الكل يعود إلى «أكتوبر» بلا
  تنبيه.
- الشريط الجانبي: «الرسوم الشهرية» و«سجل التحصيل والفواتير» و«المالية» داخل
  الطلاب ثلاث بوابات للشيء نفسه. مقبول، لكن تسمية «الرسوم الشهرية» تُخفي أن
  التسجيل ويونيو هناك أيضًا؛ «مستحقات الطلاب» أدق.
- جدول الطلاب يعرض «حذف» لكل صف بلون أحمر بجانب «المالية»؛ خطر نقرة خاطئة
  (وإن كانت محمية بتأكيد + كلمة مرور). يُقترح نقل الحذف إلى نافذة التعديل.
- قائمة «الطالب» في سجل التحصيل تُعاد بناؤها عند كل عرض (`renderPaymentHistory`)
  بمئات الخيارات؛ حقل البحث موجود، فالقائمة زائدة.
- `deleteWithPassword` يُعيد تحميل كل الشاشات (`refreshAll`) بعد حذف مصروف واحد.
- عند تسجيل الدفعة لا يُعطَّل الزر إلا في `try`؛ نقرتان سريعتان قبل الاستجابة
  تُسجّلان دفعتين (`students.js:428` يعطّل بعد التحقق، جيد، لكن لا يُعاد
  تفعيله إلا في `finally` عبر `updateStudentFeeSummary` — يعمل، فقط للتوثيق).

---

## 3. ما هو جيد ويُحافَظ عليه

- محرك رسوم واحد للمتصفح والخادم، مع اختبارات (`tests/fees.test.js`).
- التوزيع FIFO بالمعرّف يُبقي الوصولات المطبوعة ثابتة عند التعديل اللاحق.
- سقف «لا يُدفع أكثر من المستحق» للطلاب على الخادم، وتحقق الدفعات المجمّعة ككل.
- تسلسلات الوصولات (F/S/A) لا تتراجع حتى بعد الحذف.
- كشف رواتب الشهر، الوصولات الحرارية 80 مم، إشعارات أولياء الأمور، تصدير Excel
  مع اختيار الأعمدة — كلها تخدم المحاسب فعلًا.
- الصلاحيات: المشرف قراءة فقط، الإعدادات للمدير، القاعدة للمطور.
- الخادم يحمي القراءة-فقط، ويحدّ محاولات الدخول، والجلسات تنتهي.

---

## 4. ترتيب التنفيذ المقترح

1. ~~**1.3** تحققات الخادم (سقف الراتب/السلفة، الشهر، صيغة التاريخ، مبلغ المصروف)
   — صغيرة ومعزولة، مع اختبارات في `tests/db.test.js`.~~ ✅ 1.58.0 — الدفعة
   الإضافية تمرّ بعلم `extra` صريح من النموذج وتُعلَّم في السجل والوصل.
2. ~~**1.2** توحيد قراءة الشهر: اللوحة والمرشّح والوصل على التوزيع الفعلي، وإزالة
   حقل الشهر من نافذة التعديل.~~ ✅ 1.59.0 — اللوحة تعرض رقمين مسمّيين
   (المحصَّل من رسوم الشهر / المقبوض خلال الشهر كما في التقارير).
3. **2.1 + 2.2** حفظ الطالب → استمارة الرسوم → تسجيل وطباعة الوصل.
4. **1.4** كشف الرواتب: تاريخ التوظيف/التوقف والاستحقاق المخزَّن.
5. **1.5** `createdBy` على كل سجل + إلغاء بدل الحذف للوصولات.
6. **2.4 + 2.3** يومية الصندوق وطرق الدفع.
7. **1.1** إجراء السنة الجديدة (يحتاج اتفاقًا على الآلية قبل التنفيذ؛ وحتى ذلك
   الحين تحذير عند تغيير السنة الدراسية).

ملاحظة على الحالة الحالية للمستودع: تعديلات `logger.js` و`server.js` و`core.js`
(سجل الأخطاء) غير ملتزَمة بعد، والملف `ecole.jpg` ومجلد `database/` غير متتبعين؛
لم تُراجَع هنا ولم تُلمس.
