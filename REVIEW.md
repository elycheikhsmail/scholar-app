# مراجعة المشروع ومقترحات التحسين

**التاريخ:** 2026-09-09
**النطاق:** `db.js`, `server.js`, `public/app.js`, `public/index.html`, `public/style.css`, الاختبارات
**حالة الاختبارات:** `node --test tests/db.test.js` → **12/12 ناجحة**

البنية سليمة عمومًا: طبقة SQLite بمعاملات (`BEGIN IMMEDIATE`)، ونسخ احتياطية قبل المسح، وفصل نظيف بين وضع الإنتاج والتجريب. الملاحظات أدناه مرتّبة حسب الأولوية، وأغلبها يخص **جدول مستحقات الطلاب**.

---

## أولًا: أخطاء محاسبية حقيقية في احتساب المستحقات

### 1. ✅ [نُفِّذ] الطالب المسجَّل في منتصف السنة يُحمَّل رسوم كل الأشهر التسعة

`feeDueFor` في `public/app.js:113` يُرجع `monthlyFee` لأي شهر دون النظر إلى `registrationDate`، و`totalOutstandingFor` (`public/app.js:114`) يجمع الأشهر التسعة كلها. فطالب سُجِّل في فبراير يظهر مدينًا بأكتوبر ونوفمبر وديسمبر ويناير. والأسوأ أن هذا الرقم يُطبع على الإيصال نفسه في سطر «إجمالي المتبقي على الطالب» (`public/app.js:153`).

**الحل:** استحقاق = 0 للأشهر السابقة لشهر التسجيل، وإضافة حقل `leaveDate`/حالة الطالب (منقطع، محوَّل) لإيقاف تراكم الاستحقاق بعد المغادرة.

### 2. ✅ [نُفِّذ] الزيادة لا تُرحَّل إلى الشهر التالي

الدفعة مرتبطة بسلسلة نصية واحدة هي اسم الشهر. إذا دفع ولي الأمر ثلاثة أشهر دفعة واحدة تحت «أكتوبر»، يظهر أكتوبر «زاد على دفعته» بينما نوفمبر وديسمبر «لم يدفع بعد»، ولأن `totalOutstandingFor` يستعمل `Math.max(0, due-paid)` لكل شهر على حدة فإن **إجمالي المديونية يُبالغ فيه** رغم أن الطالب دفع فعلًا.

**الحل المقترح:** اعتبار الدفعة رصيدًا في حساب الطالب يُوزَّع تلقائيًا (FIFO) على أقدم الاستحقاقات غير المسدَّدة، مع عرض جدول التوزيع في كشف الطالب. هذا أهم تغيير هيكلي في الوحدة كلها.

### 3. ✅ [نُفِّذ] تعديل الرسوم يعيد كتابة الماضي

`updateStudentFees` (`db.js:418`) يستبدل `monthlyFee`، والاستحقاق يُشتق دائمًا من القيمة الحالية. فرفع الرسوم في يناير يجعل أكتوبر–ديسمبر تظهر بالرسوم الجديدة، ولا تعود الإيصالات المطبوعة مطابقة للنظام.

**الحل:** تثبيت الاستحقاق كسجل مستقل (جدول `charges`: طالب، شهر، مبلغ، تاريخ استحقاق) بدل اشتقاقه لحظيًا.

### 4. ✅ [نُفِّذ] لا توجد خصومات ولا إعفاءات

لا يوجد حقل منحة، ولا خصم إخوة، ولا إعفاء جزئي — وهي حاجة أساسية في مدرسة. حاليًا الحيلة الوحيدة هي تصفير `monthlyFee`، وهو ما يقود إلى النقطة التالية.

### 5. ✅ [نُفِّذ] الطالب المعفى يظهر «لم يدفع بعد»

في `statusFor` (`public/app.js:116`): `if(due<=0) return ['لم يدفع بعد','status-unpaid']`. من رسومه صفر يظهر كمدين. يجب أن تكون الحالة «لا توجد رسوم / معفى».

### 6. ✅ [نُفِّذ] صنف CSS مفقود: `status-unpaid`

مستعمل مرتين في `public/app.js` وغير معرَّف إطلاقًا في `public/style.css`. النتيجة أن **أهم حالة في الجدول — غير المدفوع — تُعرض بلا أي لون**، بينما «تم الدفع» و«زاد على دفعته» ملوّنة. خلل بصري مباشر في الجدول المعني.

### 7. ✅ [نُفِّذ] كود ميت وتحقق ناقص من الزيادة

في `db.js:441` يُحسب المتغير `existing` ولا يُستعمل، وفي `db.js:449` يُحسب `others` ولا يُستعمل — بقايا تحقق مُزال. النتيجة أن دفعات الطلاب تقبل أي مبلغ مهما تجاوز المستحق، بينما رواتب المدرسين **تتحقق فعلًا** من السقف (`public/app.js:175`). تناقض في السلوك؛ إما تفعيل السقف مع تحذير، أو إزالة الكود الميت. كذلك `payFee` (`public/app.js:124`) لا يقيّد المبلغ بالمتبقي.

---

## ثانيًا: تحسينات وظيفية معتبرة على الجدول

### 8. ✅ [نُفِّذ] الجدول يعرض شهرًا واحدًا فقط — وهذا أكبر قيد فيه

لمعرفة من عليه ديون يجب على المحاسب تصفّح تسعة أشهر يدويًا واحدًا واحدًا. المقترح: **وضع «إجمالي المستحقات»** في قائمة `feeMonth` — سطر واحد لكل طالب يعرض إجمالي المستحق / إجمالي المدفوع / إجمالي المتبقي / أقدم شهر غير مسدَّد / عدد الأشهر المتأخرة. الدالة `totalOutstandingFor` موجودة أصلًا ولا تُستعمل في الجدول.

### 9. ✅ [نُفِّذ] لا فرز ولا تصفية حسب الحالة

إضافة فرز بالنقر على رؤوس الأعمدة (المتبقي تنازليًا خصوصًا)، ومرشّح حالة (متأخر فقط / غير مدفوع / مسدَّد / زيادة)، وحدّ أدنى للمديونية.

### 10. ✅ [نُفِّذ] عمود «عمر الدَّين»

متأخر منذ 30 / 60 / 90 يومًا — معيار في متابعة الذمم، ويغني عن الاعتماد على لون الصف فقط.

### 11. ✅ [نُفِّذ] مجاميع فرعية لكل قسم

إلى جانب المجموع العام في `feeTotals`، مع تقرير «أعلى المديونين».

### 12. ✅ [نُفِّذ] تصدير وطباعة

لا يوجد تصدير CSV/Excel ولا طباعة لكشف مستحقات القسم. وبما أن `guardianName` و`guardianPhone` مسجَّلان، يمكن توليد **قائمة إشعارات لأولياء الأمور** (رسائل تذكير جاهزة للنسخ إلى واتساب/SMS) وطباعة إشعار فردي بالمتبقي.

---

## ثالثًا: الأداء (سيظهر مع أول سنة بيانات حقيقية)

### 13. ✅ [نُفِّذ] `renderFees` يعيد مسح كل الدفعات أربع مرات لكل طالب

`paidFor` (`public/app.js:112`) يمسح `studentPayments` كاملًا، ويُستدعى لكل طالب مباشرةً، ثم داخل `lateFor`، ثم مرتين داخل `statusFor`. التعقيد `O(الطلاب × الدفعات × 4)`. ببيانات العرض (144 طالبًا × 432 دفعة) ≈ 250 ألف عملية، لكن بمدرسة من 800 طالب و8000 دفعة تصبح ≈ 25 مليون عملية **عند كل ضغطة مفتاح في خانة البحث**.

**الحل:** بناء `Map` بمفتاح `studentId|month` مرة واحدة في بداية كل عرض.

### 14. ✅ [نُفِّذ] `renderPaymentHistory` يُنفَّذ مرتين عند تغيير الشهر

`public/app.js:121` يستدعيه، و`renderFees` يستدعيه مجددًا في آخر سطر — وهو يعيد بناء قائمة `<select>` لكل الطلاب في كل مرة.

### 15. ✅ [نُفِّذ] لا تهدئة (debounce) لحقل البحث

ولا ترقيم صفحات؛ كل الصفوف تُبنى بـ `innerHTML` داخل إطار ارتفاعه 450px.

### 16. ✅ [نُفِّذ] كل عملية كتابة في `db.js` تمرّ على قاعدة البيانات كاملة مرتين

الغلاف في `db.js:567` ينفّذ `readData()` (تحليل JSON لكل سجل في كل الجداول)، ثم `save()` (`db.js:145`) يقرأ كل صفوف كل جدول مجددًا للمقارنة. تسجيل دفعة واحدة = مرور كامل مضاعف على القاعدة. يستحق الانتقال إلى استعلامات موجّهة (`WHERE id = ?`) للمسارات الساخنة.

### 17. ✅ [نُفِّذ] `/api/data` يرسل قاعدة البيانات كاملة إلى المتصفح

(طلاب، دفعات، مدرسون، امتحانات) بعد **كل** دفعة تُسجَّل، لأن `payFee` يستدعي `load()`. يُقترح نقاط نهاية مخصصة مثل `/api/fees/summary?month=&department=` مع الحساب في SQL.

---

## رابعًا: ✅ [نُفِّذ] ملاحظات أمنية ثانوية

- ~~**الجلسات لا تنتهي أبدًا**~~ ✅ تنتهي الجلسة بعد 12 ساعة (`SCHOOL_SESSION_TTL_MS` لتغيير المدة).
- ~~**`Access-Control-Allow-Origin: *`**~~ ✅ صار الرأس يُمنح فقط للمصدر المحلي الذي يخدم الواجهة.
- ~~**لا تحديد لمعدل المحاولات**~~ ✅ بعد 8 محاولات فاشلة خلال 5 دقائق من العنوان نفسه تُرفض المحاولات حتى انتهاء النافذة.
- ~~`western()` لا يشمل الفارسية~~ ✅ صار يحوّل `٠-٩` و`۰-۹` معًا.

---

## حالة التنفيذ

كل نقاط هذه المراجعة نُفِّذت. التغطية: `node --test tests/db.test.js tests/fees.test.js tests/seed-demo.test.js` (29 اختبارًا) و`npx playwright test`، إضافة إلى تشغيل يدوي للواجهة على نسخة منفصلة ببيانات تجريبية.

## خطة البدء المقترحة

أعلى أثر مقابل أقل مخاطرة، بالترتيب:

1. ~~**إصلاح `.status-unpaid` المفقود + حالة الطالب المعفى** (النقطتان 5 و6).~~ ✅ **نُفِّذ**
2. ~~**خريطة الدفعات (`Map`) في `renderFees` وإزالة الاستدعاء المكرر** (النقطتان 13 و14).~~ ✅ **نُفِّذ** — 313ms ← 4ms لكل عرض عند 800 طالب و7200 دفعة.
3. **عدم تحميل الطالب رسوم الأشهر السابقة لتسجيله** — يصحّح رقمًا مطبوعًا على الإيصالات (النقطة 1).
4. **وضع «إجمالي المستحقات» في الجدول مع الفرز والتصفية بالحالة** — الميزة الأكثر طلبًا عمليًا (النقطتان 8 و9).

~~أما **ترحيل الزيادة** (النقطة 2) و**تثبيت الاستحقاق تاريخيًا** (النقطة 3) فهما تغييران في نموذج البيانات ويستحقان تنفيذًا منفصلًا بعد الاتفاق على الآلية.~~ ✅ **نُفِّذا** مع النقطة 1 في محرك `public/fees.js`: نافذة القيد (تسجيل ← مغادرة)، وسجل فترات الرسوم، وتوزيع الدفعات FIFO على أقدم استحقاق. راجع قسم « Calcul des frais dus » في `README.md`.

**تغيير سلوك مصاحب:** أُلغيت مهلة الشهر الواحد في تاريخ الاستحقاق. كانت تدفع استحقاق أول شهر إلى تاريخ الشهر التالي (فيتساوى تاريخا شهرين)، وكانت أصلًا تعويضًا عن تحميل أشهر ما قبل التسجيل — وقد زال سببها. صار كل شهر يستحق في يوم التسجيل من ذلك الشهر.
