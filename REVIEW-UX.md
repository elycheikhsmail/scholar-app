# مراجعة انسجام تجربة المستخدم

مراجعة مبنية على `CHANGEMENTS.md` (حتى الإصدار 1.4.0) وعلى قراءة كاملة لملفات
الواجهة: `public/index.html`، `public/style.css`، `public/core.js`،
`public/students.js`، `public/fees-ui.js`، `public/staff.js`، `public/reports.js`،
`public/settings.js`، `public/exams.js`، `public/excel.js`.

**تاريخ المراجعة:** 2026-09-10 — **الإصدار المراجَع:** 1.4.0

## الخلاصة

الإصدارات 1.1.0 → 1.4.0 حسّنت **شاشة الرسوم** وحدها (تصفية سريعة، اختيار أعمدة،
نافذة منبثقة، تصدير)، فأصبحت متقدمة على بقية الشاشات. أغلب التنافر اليوم هو
«هجرة غير مكتملة»: نمط جديد طُبّق في مكان واحد وتُرك النمط القديم في الباقي.

---

## 1. ثلاثة أنماط مختلفة للنموذج نفسه (الأهم)

| الشاشة | النمط الحالي |
| --- | --- |
| الموظفون (1.2.5) | نافذة منبثقة + زر «+ إضافة موظف» |
| رسوم الطالب (1.2.2) | نافذة منبثقة |
| الطلاب، المصروفات | نموذج ثابت أعلى الصفحة + وميض `showEditForm` |
| الأقسام، الدفعات، الرواتب، السلف | سلسلة نوافذ `askInput` متتالية |

الأسوأ هو النمط الثالث:

- `editStudentPayment` يسأل عن المبلغ ثم التاريخ في نافذتين متتاليتين.
- `editSalaryPayment` يفتح ثلاث نوافذ (المبلغ، التاريخ، عدد الساعات).
- `editAdvance` يفتح ثلاث نوافذ (المبلغ، التاريخ، الملاحظات).
- `editDepartment` يفتح نافذتين (الاسم، الرسوم).
- التاريخ يُكتب **نصًّا** بصيغة `YYYY-MM-DD` بينما كل النماذج الأخرى تستعمل
  `<input type="date">`.
- لا يمكن التراجع في منتصف السلسلة ولا مراجعة القيم قبل الحفظ.

**المطلوب:** دالة واحدة `openRecordDialog(config)` في `core.js` تُستعمل في كل
الشاشات، ونقل الطلاب والمصروفات والأقسام والدفعات والرواتب والسلف إليها، مع زر
`+ إضافة` في `section-head` لكل شاشة (موجود في الموظفين فقط حاليًا).

## 2. الإجراء الواحد بمظهرين

فتح استمارة الرسوم يتم بزر نصي `رسوم الطالب` (`.btn-pay`) في جدول الطلاب،
وبأيقونة `+` دائرية (`.fee-form-trigger`) في جدول الرسوم — والاثنان ينادِيان
`openStudentFees(id)`. التغيير 1.2.4 طبّق الأيقونة في مكان واحد فقط.

وكذلك زر «تعديل» في جدول الموظفين يفتح نافذة منبثقة، بينما «تعديل» في جدول
الطلاب يقفز بالصفحة ويومض على النموذج.

## 3. لا حماية من النقر المزدوج

`disabled = true` أثناء الطلب موجود في `studentFeesForm` و`studentFeePaymentForm`
و`applicationModeForm` فقط. النماذج الأخرى (الطالب، الموظف، المصروف، الراتب،
السلفة، القسم، الإعدادات، الامتحان) قابلة للإرسال مرتين، ما يُنتج **سجلات
مكرّرة فعليًا**.

**المطلوب:** مُغلِّف مشترك `submitForm(form, handler)` يعطّل زر الإرسال ويعيد
تفعيله في `finally`.

## 4. ثلاث مفردات لنفس الحالة

| المصدر | المفردات |
| --- | --- |
| `statusFor` (شهر واحد) | `تم الدفع` / `لم يدفع بعد` / `هناك باقي` / `لا توجد رسوم` |
| `rowStatus` (وضع الإجمالي) | `مسدَّد بالكامل` / `غير مسدَّد: n` |
| قائمة `feeStatus` | `عليه متبقٍّ` / `متأخر فقط` / `مسدَّد بالكامل` / `بلا رسوم` |
| شرائح التصفية السريعة | `عليه متبقٍّ` / `متأخر` / `مسدَّد` |

المستخدم يرى ثلاث كلمات مختلفة للحالة نفسها داخل شاشة واحدة.

**المطلوب:** جدول مصطلحات واحد في `fees.js` تستعمله كل الدوال والقوائم.

## 5. أدوات الجداول غير متكافئة

| الجدول | بحث | فرز | تصفية | سقف صفوف | تصدير |
| --- | --- | --- | --- | --- | --- |
| الرسوم | ✅ | ✅ | ✅ غنية | ✅ 300 | CSV |
| الطلاب | ✅ | ❌ | القسم فقط | ❌ | Excel |
| التحصيل | ✅ | ❌ | ✅ | ❌ | ❌ |
| الموظفون | ❌ | ❌ | ❌ | ❌ | ❌ |
| الرواتب | ❌ | ❌ | ❌ | ❌ | ❌ |
| السلف | ❌ | ❌ | ❌ | ❌ | ❌ |
| المصروفات | ❌ | ❌ | ❌ | ❌ | ❌ |

جدولا المصروفات والرواتب ينموان بلا حدّ ولا وسيلة للبحث فيهما.

**المطلوب:** شريط أدوات مشترك (بحث + عدّاد + مسح التصفية + فرز + سقف صفوف)
يُركَّب على كل جدول.

## 6. التصدير والطباعة

### التصدير

- الطلاب ← ملف `.xlsx` حقيقي مع نافذة اختيار أعمدة محفوظة محليًا (1.4.0).
- الرسوم ← ملف `.csv` بفاصلة منقوطة، و**لا يحترم الأعمدة المخفية**: الدالة
  `feeSheetRows` تبني رأسًا ثابتًا يتجاهل `feeHiddenColumns` تمامًا، فما يراه
  المستخدم ليس ما يُصدَّر.
- بقية الشاشات (الموظفون، الرواتب، السلف، المصروفات، التحصيل) بلا تصدير.

الدالة `downloadXlsx` متاحة عالميًا في `excel.js`، فالأولى توحيد الجميع عليها مع
نافذة اختيار الأعمدة نفسها.

### الطباعة

ثلاث آليات مختلفة وثلاثة رؤوس مختلفة:

1. `openPrintWindow` — كشف الرسوم وإشعارات أولياء الأمور.
2. نافذة جديدة بطباعة تلقائية بعد 250ms — إيصال الدفع.
3. حقن `#printSheet` في الصفحة ثم `window.print()` — كشوف الامتحانات.

**المطلوب:** نقل `openPrintWindow` من `fees-ui.js` إلى `core.js` وتمرير كل
الطباعات عبرها برأس موحّد (اسم المدرسة، السنة، شارة وضع التجريب).

## 7. سياسة التأكيد وكلمة المرور غير موحّدة

- حذف أو تعديل الطالب والموظف والمصروف والقسم والدفعة: تأكيد + كلمة مرور.
- حذف قاعدة ملاحظة أو قرار (`deleteRemarkRule` / `deleteDecisionRule`):
  **بلا تأكيد، بلا كلمة مرور، بلا رسالة نجاح، وبلا `try/catch`** — أي فشل في
  الشبكة يمرّ صامتًا دون علم المستخدم. نفس المشكلة في `addRemarkRule` و
  `addDecisionRule`.
- حفظ رسوم الطالب يطلب كلمة المرور، بينما تسجيل دفعة (وهي عملية مالية) لا يطلبها.
- تعديل اسم المدرسة يفرض إدخال «كلمة المرور الحالية» لأن الحقل مدموج في نموذج
  تغيير كلمة المرور نفسه.

## 8. سلوك النوافذ المنبثقة

- `studentExportDialog` لا يُغلق بالنقر خارجه، بينما بقية النوافذ تُغلق، ولا
  يُصفَّر عند الإغلاق.
- `studentFeesPanel`: التنظيف (`selectedFeeStudentId = null`) مربوط بزر الإغلاق
  فقط، فالإغلاق بـ `Esc` يترك الحالة قديمة. الصحيح نقله إلى `onclose` كما فُعل
  في `teacherDialog`.
- التركيز الأولي مختلف في كل نافذة: الموظف ← حقل الاسم، كشف الحساب ← زر
  الإغلاق، الرسوم ← أول حقل، التصدير ← لا شيء.
- في `style.css` أربع كتل نوافذ شبه متطابقة مكرّرة سطرًا بسطر:
  `.fee-form-dialog` و`.export-dialog` و`.staff-dialog` و`.ledger-dialog`.

## 9. السبب الجذري: لا توجد رموز تصميم

`style.css` فيه **صفر** متغيرات CSS، مع تكرار:

- `#168a4a` ٢٥ مرة
- `#1769aa` ٩ مرات
- `border-radius:9px` ١١ مرة

لذلك كل ميزة جديدة تُعيد اختراع مظهرها وتنحرف قليلًا عن سابقتها.

**المطلوب:** تعريف `:root` بالألوان والحواف والظلال والمسافات، وهو ما يمنع تكرار
هذا التنافر مستقبلًا.

## 10. تفاصيل أصغر لكنها مرئية

- رأس عمود الإجراءات: `إجراءات` في ٦ جداول و`إجراء` في ٣.
- جداول بلا رسالة فراغ فتظهر فارغة تمامًا: الطلاب، المصروفات، الأقسام، قواعد
  الملاحظات، قواعد القرارات. بقية الجداول فيها رسالة.
- `western()` مطبّقة على التواريخ في الرواتب والسلف والمصروفات والتحصيل، وغير
  مطبّقة في كشف الطالب وسجل الامتحانات وعمود «تاريخ المغادرة».
- «أوقية» تظهر في الإيصال وملخص الطالب والإشعارات، وتغيب عن كل الجداول وبطاقات
  الرئيسية والتقارير.
- `#toast` بلا `role="status"`، فلا يُنطق أي نجاح أو خطأ لقارئ الشاشة، رغم أن
  `feesFilterSummary` و`collectionTotals` تستعملان `aria-live`.
- «الرسوم» و«التحصيل» يشتركان في اللون `#168a4a` نفسه في القائمة الجانبية،
  فينكسر الترميز اللوني للأقسام.
- `min-width:1250px` مفروض على **كل** جدول، فجدول الأقسام (٤ أعمدة) يتطلب
  تمريرًا أفقيًا على الجوال بلا سبب.
- حدود دنيا متضاربة للمبالغ: دفعة الطالب `min=0.01`، والمصروف والراتب والسلفة
  `min=1`.
- `applyApplicationMode` يستبدل `document.title` بـ
  `وضع الإنتاج — حسابات المدرسة` فيسقط اسم المدرسة الموجود في العنوان الأصلي.
- `type="search"` مستعمل في حقل التحصيل فقط، فزر المسح (×) يظهر في شاشة واحدة.
- زر «إلغاء» في نافذة الموظف مقابل «تفريغ النموذج» في الطلاب والمصروفات، لنفس
  الوظيفة تقريبًا.

---

## ترتيب مقترح للتنفيذ

| الإصدار | المحتوى |
| --- | --- |
| **1.5.0** ✅ | *منفَّذ* — رموز CSS موحّدة (`:root`) + كتلة نوافذ واحدة (`.app-dialog`) + `role="status"` للـ toast + حالات الفراغ الناقصة + توحيد «إجراءات» ومفردات الحالة + لون واحد لكل شاشة |
| **1.6.0** | `openRecordDialog` مشترك، ونقل الطلاب والمصروفات والأقسام إليه، وإلغاء سلاسل `askInput` للدفعات والرواتب والسلف، وتعطيل أزرار الحفظ أثناء الطلب |
| **1.7.0** | شريط أدوات جداول مشترك (بحث/فرز/سقف صفوف) لكل الشاشات، وتوحيد التصدير على `downloadXlsx` مع احترام الأعمدة الظاهرة |
| **1.8.0** | توحيد الطباعة عبر `openPrintWindow`، وسياسة تأكيد/كلمة مرور واحدة مطبّقة على قواعد الامتحانات أيضًا |
